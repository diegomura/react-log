import BaseElement from './BaseElement';

export default class Span extends BaseElement {
  getDefaultStyles() {
    return {
      color: 'black',
    };
  }

  render() {
    return super.render();
  }
}
